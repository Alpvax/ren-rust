/*use super::{
    helper::*,
    parse_expression,
    ExpressionError::{self, *},
    Lexer, Token,
};
use crate::ast::expression::Expression;

make_test_fn!(test_expr<Expression, ExpressionError> = parse_expression);

#[test]
fn parse_empty() {
    assert_err(test_expr(""), NoTokens, 0);
}*/
